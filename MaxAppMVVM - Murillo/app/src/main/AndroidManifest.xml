<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.github.tumusx.maxappmvvm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MaxiAppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_maximatech_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_maximatech_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaxAppMVVM"
        tools:targetApi="31">
        <activity
            android:name=".clients.ClientsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".menu.MenuActivity"
            android:exported="false" />
        <activity
            android:name=".splash.SplashHomeActivity"
            android:exported="true"
            android:theme="@style/SplashScreenHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>